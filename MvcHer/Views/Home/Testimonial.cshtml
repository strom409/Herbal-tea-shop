@model IEnumerable<MvcHer.Models.Testimonial>

@{
    ViewData["Title"] = "Customer Testimonials - TEA HOUSE";
    var testimonials = Model?.ToList() ?? new List<MvcHer.Models.Testimonial>();
}

<!-- Testimonials Section with Same Design as Home Page -->
<div class="container-xxl py-5">
    <div class="container">
        <!-- Section Header -->
        <div class="text-center mx-auto mb-5" style="max-width: 500px;">
            <div class="testimonial-badge mb-3">
                <span class="badge-text">✨ Customer Stories</span>
            </div>
            <h1 class="testimonial-main-title mb-4">
                What Our <span class="highlight-text-green">Customers</span> Say
            </h1>
            <p class="testimonial-subtitle">
                Discover authentic experiences from our valued customers who have fallen in love with our premium tea collection
            </p>
        </div>

        <!-- Trust Indicators -->
        <div class="row g-4 mb-5">
            <div class="col-lg-3 col-md-6">
                <div class="trust-indicator text-center">
                    <div class="trust-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="trust-number">@testimonials.Count()+</h3>
                    <p class="trust-label">Happy Customers</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="trust-indicator text-center">
                    <div class="trust-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <h3 class="trust-number">@(testimonials.Any() ? testimonials.Average(t => t.Rating).ToString("F1") : "4.9")</h3>
                    <p class="trust-label">Average Rating</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="trust-indicator text-center">
                    <div class="trust-icon">
                        <i class="fas fa-shipping-fast"></i>
                    </div>
                    <h3 class="trust-number">24h</h3>
                    <p class="trust-label">Fast Delivery</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="trust-indicator text-center">
                    <div class="trust-icon">
                        <i class="fas fa-leaf"></i>
                    </div>
                    <h3 class="trust-number">100%</h3>
                    <p class="trust-label">Organic</p>
                </div>
            </div>
        </div>

        @if (testimonials.Any())
        {
            <!-- Testimonials Carousel with Same Design as Home Page -->
            <div class="testimonials-carousel owl-carousel owl-theme">
                @foreach (var testimonial in testimonials)
                {
                    <div class="testimonial-card">
                        <div class="testimonial-content">
                            <div class="testimonial-header">
                                <div class="customer-photo">
                                    @if (!string.IsNullOrEmpty(testimonial.ClientImageUrl))
                                    {
                                        <img src="@testimonial.ClientImageUrl" alt="@testimonial.ClientName" />
                                    }
                                    else
                                    {
                                        <div class="photo-placeholder">
                                            <i class="fas fa-user"></i>
                                        </div>
                                    }
                                </div>
                                <div class="customer-info">
                                    <h4 class="customer-name">@testimonial.ClientName</h4>
                                    <div class="customer-rating">
                                        @for (int i = 1; i <= 5; i++)
                                        {
                                            @if (i <= testimonial.Rating)
                                            {
                                                <i class="fas fa-star"></i>
                                            }
                                            else
                                            {
                                                <i class="far fa-star"></i>
                                            }
                                        }
                                    </div>
                                    @if (!string.IsNullOrEmpty(testimonial.ClientProfession))
                                    {
                                        <p class="customer-profession">@testimonial.ClientProfession</p>
                                    }
                                </div>
                                @if (testimonial.IsFeatured)
                                {
                                    <div class="featured-badge">
                                        <i class="fas fa-crown"></i>
                                        <span>Featured</span>
                                    </div>
                                }
                            </div>
                            <div class="testimonial-text">
                                <i class="fas fa-quote-left quote-icon"></i>
                                <p>@testimonial.TestimonialText</p>
                            </div>
                            @if (testimonial.IsApproved)
                            {
                                <div class="verified-badge">
                                    <i class="fas fa-check-circle"></i>
                                    <span>Approved Review</span>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <!-- Empty State -->
            <div class="text-center py-5">
                <div class="empty-state">
                    <i class="fas fa-comments fa-3x text-muted mb-3"></i>
                    <h3 class="text-muted">No Testimonials Yet</h3>
                    <p class="text-muted">Be the first to share your experience with our premium tea collection!</p>
                    <a asp-controller="Store" asp-action="Index" class="btn btn-primary mt-3">
                        <i class="fas fa-shopping-cart me-2"></i>Shop Now
                    </a>
                </div>
            </div>
        }
    </div>
</div>

<!-- Add the same beautiful testimonials section styling as home page -->
@{
    var testimonialsList = Model as List<MvcHer.Models.Testimonial> ?? new List<MvcHer.Models.Testimonial>();
}

@if (testimonialsList.Any())
{
    <!-- Testimonials Section with Dark Background -->
    <div class="testimonials-section">
        <!-- Floating Leaves Animation -->
        <div class="floating-leaf">🍃</div>
        <div class="floating-leaf">🌿</div>
        <div class="floating-leaf">🍃</div>
        <div class="floating-leaf">🌿</div>
        <div class="floating-leaf">🍃</div>
        
        <div class="container">
            <div class="text-center mx-auto mb-5" style="max-width: 500px;">
                <div class="testimonial-badge mb-3">
                    <span class="badge-text">✨ Customer Stories</span>
                </div>
                <h1 class="testimonial-main-title mb-4" style="color: white;">
                    What Our <span class="highlight-text-green">Customers</span> Say
                </h1>
                <p class="testimonial-subtitle" style="color: rgba(255,255,255,0.8);">
                    Discover authentic experiences from our valued customers who have fallen in love with our premium tea collection
                </p>
            </div>

            <!-- Testimonials Carousel -->
            <div class="testimonials-carousel owl-carousel owl-theme">
                @foreach (var testimonial in testimonialsList)
                {
                    <div class="testimonial-card">
                        <div class="testimonial-content">
                            <div class="testimonial-header">
                                <div class="customer-photo">
                                    @if (!string.IsNullOrEmpty(testimonial.ClientImageUrl))
                                    {
                                        <img src="@testimonial.ClientImageUrl" alt="@testimonial.ClientName" />
                                    }
                                    else
                                    {
                                        <div class="photo-placeholder">
                                            <i class="fas fa-user"></i>
                                        </div>
                                    }
                                </div>
                                <div class="customer-info">
                                    <h4 class="customer-name">@testimonial.ClientName</h4>
                                    <div class="customer-rating">
                                        @for (int i = 1; i <= 5; i++)
                                        {
                                            @if (i <= testimonial.Rating)
                                            {
                                                <i class="fas fa-star"></i>
                                            }
                                            else
                                            {
                                                <i class="far fa-star"></i>
                                            }
                                        }
                                    </div>
                                    @if (!string.IsNullOrEmpty(testimonial.ClientProfession))
                                    {
                                        <p class="customer-profession">@testimonial.ClientProfession</p>
                                    }
                                </div>
                                @if (testimonial.IsFeatured)
                                {
                                    <div class="featured-badge">
                                        <i class="fas fa-crown"></i>
                                        <span>Featured</span>
                                    </div>
                                }
                            </div>
                            <div class="testimonial-text">
                                <i class="fas fa-quote-left quote-icon"></i>
                                <p>@testimonial.TestimonialText</p>
                            </div>
                            @if (testimonial.IsApproved)
                            {
                                <div class="verified-badge">
                                    <i class="fas fa-check-circle"></i>
                                    <span>Approved Review</span>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Individual Testimonials Grid Section -->
    <section class="py-5" style="background: #f8f9fa;">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="section-title" style="color: #2c3e50; font-size: 2.5rem; font-weight: 700;">All Customer Reviews</h2>
                <p class="section-subtitle" style="color: #666; font-size: 1.1rem;">Read authentic experiences from our valued customers</p>
            </div>
            
            <div class="row">
                @foreach (var testimonial in testimonialsList.OrderByDescending(t => t.IsFeatured).ThenByDescending(t => t.CreatedAt))
                {
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="individual-testimonial-card">
                            @if (testimonial.IsFeatured)
                            {
                                <div class="featured-review-badge">
                                    <i class="fas fa-crown me-1"></i>Featured Review
                                </div>
                            }
                            
                            <div class="testimonial-content-wrapper">
                                <div class="customer-profile">
                                    <div class="customer-avatar">
                                        @if (!string.IsNullOrEmpty(testimonial.ClientImageUrl))
                                        {
                                            <img src="@testimonial.ClientImageUrl" alt="@testimonial.ClientName" />
                                        }
                                        else
                                        {
                                            <div class="avatar-placeholder">
                                                <i class="fas fa-user"></i>
                                            </div>
                                        }
                                    </div>
                                    <div class="customer-details">
                                        <h4 class="customer-name">@testimonial.ClientName</h4>
                                        @if (!string.IsNullOrEmpty(testimonial.ClientProfession))
                                        {
                                            <p class="customer-profession">@testimonial.ClientProfession</p>
                                        }
                                        <div class="star-rating">
                                            @for (int i = 1; i <= 5; i++)
                                            {
                                                @if (i <= testimonial.Rating)
                                                {
                                                    <i class="fas fa-star"></i>
                                                }
                                                else
                                                {
                                                    <i class="far fa-star"></i>
                                                }
                                            }
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="testimonial-quote">
                                    <i class="fas fa-quote-left quote-icon"></i>
                                    <p class="testimonial-description">@testimonial.TestimonialText</p>
                                </div>
                                
                                @if (!string.IsNullOrEmpty(testimonial.ClientCompany))
                                {
                                    <div class="customer-company">
                                        <i class="fas fa-building me-1"></i>@testimonial.ClientCompany
                                    </div>
                                }
                                
                                @if (!string.IsNullOrEmpty(testimonial.ClientLocation))
                                {
                                    <div class="customer-location">
                                        <i class="fas fa-map-marker-alt me-1"></i>@testimonial.ClientLocation
                                    </div>
                                }
                                
                                @if (testimonial.IsApproved)
                                {
                                    <div class="verified-review">
                                        <i class="fas fa-check-circle me-1"></i>Verified Review
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </section>

    <!-- Statistics Section -->
    <section class="stats-section">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="stat-value">@testimonialsList.Count()</div>
                    <div class="stat-description">Happy Customers</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="stat-value">@(testimonialsList.Any() ? testimonialsList.Average(t => t.Rating).ToString("F1") : "4.9")</div>
                    <div class="stat-description">Average Rating</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-heart"></i>
                    </div>
                    <div class="stat-value">@testimonialsList.Count(t => t.IsFeatured)</div>
                    <div class="stat-description">Featured Reviews</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-leaf"></i>
                    </div>
                    <div class="stat-value">100%</div>
                    <div class="stat-description">Organic Quality</div>
                </div>
            </div>
        </div>
    </section>


}
else
{
    <!-- Empty State -->
    <section class="testimonials-section">
        <div class="container">
            <div class="text-center py-5">
                <i class="fas fa-comments fa-5x mb-4" style="color: #8BC34A;"></i>
                <h3 class="mb-3" style="color: white;">No Testimonials Found</h3>
                <p class="mb-4" style="color: rgba(255,255,255,0.8);">
                    @{
                        var totalCount = ViewBag.TotalTestimonials ?? 0;
                    }
                    @if (totalCount > 0)
                    {
                        <span>We found @totalCount testimonials in the database, but they may need approval or activation.</span>
                    }
                    else
                    {
                        <span>Be the first to share your experience with our premium herbal teas!</span>
                    }
                </p>
                <div class="d-flex justify-content-center gap-3">
                    <a href="@Url.Action("Index", "Home")" class="btn btn-success btn-lg">
                        <i class="fas fa-home me-2"></i>Back to Home
                    </a>
                    <a href="@Url.Action("Contact", "Home")" class="btn btn-outline-light btn-lg">
                        <i class="fas fa-envelope me-2"></i>Contact Us
                    </a>
                </div>
            </div>
        </div>
    </section>
}

<!-- Call to Action Section -->
<section class="cta-section">
    <div class="container">
        <div class="cta-content">
            <h2 class="cta-title">Share Your Tea Experience</h2>
            <p class="cta-subtitle">Join thousands of satisfied customers and tell us about your favorite herbal tea moments</p>
            <div class="cta-buttons">
                <a href="@Url.Action("Index", "Store")" class="cta-btn">
                    <i class="fas fa-shopping-cart me-2"></i>Shop Premium Teas
                </a>
                <a href="@Url.Action("Contact", "Home")" class="cta-btn secondary">
                    <i class="fas fa-envelope me-2"></i>Share Your Story
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    /* Testimonials Section Styling - Same as Home Page */
    .testimonials-section {
        background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
        position: relative;
        overflow: hidden;
        padding: 80px 0;
    }

    .testimonials-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="%23ffffff" opacity="0.05"><polygon points="0,0 1000,0 1000,100 0,100"/></svg>');
        pointer-events: none;
    }

    .testimonial-badge {
        display: inline-block;
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        border-radius: 50px;
        padding: 12px 30px;
        box-shadow: 0 8px 25px rgba(139, 195, 74, 0.3);
    }

    .testimonial-badge .badge-text {
        color: white;
        font-weight: 600;
        font-size: 1rem;
        letter-spacing: 0.5px;
    }

    .testimonial-main-title {
        font-size: 3rem;
        font-weight: 700;
        color: #2c3e50;
        font-family: 'Playfair Display', serif;
        line-height: 1.2;
    }

    .highlight-text-green {
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .testimonial-subtitle {
        font-size: 1.2rem;
        color: #666;
        font-weight: 300;
        line-height: 1.6;
    }

    /* Trust Indicators */
    .trust-indicator {
        background: white;
        padding: 30px 20px;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        height: 100%;
    }

    .trust-indicator:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }

    .trust-icon {
        width: 70px;
        height: 70px;
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 20px;
        box-shadow: 0 8px 25px rgba(139, 195, 74, 0.3);
    }

    .trust-icon i {
        font-size: 1.5rem;
        color: white;
    }

    .trust-number {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 10px;
        font-family: 'Playfair Display', serif;
    }

    .trust-label {
        color: #666;
        font-weight: 500;
        margin: 0;
    }

    /* Testimonials Carousel */
    .testimonials-carousel {
        margin-top: 60px;
    }

    .testimonial-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 20px;
        padding: 30px;
        margin: 15px;
        transition: all 0.4s ease;
        position: relative;
        overflow: hidden;
    }

    .testimonial-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(139, 195, 74, 0.1) 0%, rgba(104, 159, 56, 0.1) 100%);
        opacity: 0;
        transition: opacity 0.3s ease;
        pointer-events: none;
    }

    .testimonial-card:hover::before {
        opacity: 1;
    }

    .testimonial-card:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 25px 50px rgba(139, 195, 74, 0.3);
        border-color: rgba(139, 195, 74, 0.4);
    }

    .testimonial-header {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
        position: relative;
        z-index: 2;
    }

    .customer-photo {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        overflow: hidden;
        margin-right: 15px;
        border: 3px solid rgba(139, 195, 74, 0.3);
        transition: all 0.3s ease;
    }

    .testimonial-card:hover .customer-photo {
        border-color: #8BC34A;
        transform: scale(1.1);
    }

    .customer-photo img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .photo-placeholder {
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.5rem;
    }

    .customer-info {
        flex: 1;
    }

    .customer-name {
        color: white;
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 5px;
        font-family: 'Playfair Display', serif;
    }

    .customer-rating {
        display: flex;
        gap: 2px;
    }

    .customer-rating i {
        color: #ffd700;
        font-size: 0.9rem;
    }

    .featured-badge {
        background: linear-gradient(135deg, #ffd700 0%, #ffb300 100%);
        color: #2c3e50;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 5px;
        box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
    }

    .testimonial-text {
        position: relative;
        z-index: 2;
        margin-bottom: 20px;
    }

    .quote-icon {
        color: rgba(139, 195, 74, 0.3);
        font-size: 2rem;
        position: absolute;
        top: -10px;
        left: -5px;
    }

    .testimonial-text p {
        color: rgba(255, 255, 255, 0.9);
        font-style: italic;
        line-height: 1.6;
        margin: 0;
        padding-left: 25px;
        font-size: 1.1rem;
    }

    .verified-badge {
        background: rgba(34, 197, 94, 0.2);
        color: #22c55e;
        padding: 8px 15px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        border: 1px solid rgba(34, 197, 94, 0.3);
        position: relative;
        z-index: 2;
    }

    /* Individual Testimonial Cards */
    .individual-testimonial-card {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        position: relative;
        height: 100%;
        border: 1px solid #e9ecef;
    }

    .individual-testimonial-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }

    .featured-review-badge {
        position: absolute;
        top: -10px;
        right: 20px;
        background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
        color: white;
        padding: 8px 15px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
    }

    .customer-profile {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
    }

    .customer-avatar {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        overflow: hidden;
        margin-right: 15px;
        border: 3px solid #8BC34A;
        flex-shrink: 0;
    }

    .customer-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .avatar-placeholder {
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.5rem;
    }

    .customer-details {
        flex: 1;
    }

    .customer-name {
        font-size: 1.2rem;
        font-weight: 700;
        color: #2c3e50;
        margin: 0 0 5px 0;
        font-family: 'Playfair Display', serif;
    }

    .customer-profession {
        color: #8BC34A;
        font-size: 0.9rem;
        font-weight: 500;
        margin: 0 0 8px 0;
    }

    .star-rating {
        display: flex;
        gap: 2px;
    }

    .star-rating i {
        color: #FFD700;
        font-size: 0.9rem;
    }

    .testimonial-quote {
        margin-bottom: 20px;
        position: relative;
    }

    .testimonial-quote .quote-icon {
        color: #8BC34A;
        font-size: 1.5rem;
        margin-bottom: 10px;
    }

    .testimonial-description {
        font-size: 1rem;
        line-height: 1.6;
        color: #555;
        font-style: italic;
        margin: 0;
        text-align: justify;
    }

    .customer-company,
    .customer-location {
        font-size: 0.85rem;
        color: #666;
        margin-bottom: 8px;
    }

    .customer-company i,
    .customer-location i {
        color: #8BC34A;
        width: 15px;
    }

    .verified-review {
        display: inline-flex;
        align-items: center;
        background: #e8f5e8;
        color: #2e7d32;
        padding: 5px 12px;
        border-radius: 15px;
        font-size: 0.8rem;
        font-weight: 500;
        margin-top: 15px;
    }

    .verified-review i {
        color: #4caf50;
    }

    /* Floating Leaves Animation */
    .floating-leaf {
        position: absolute;
        font-size: 2rem;
        opacity: 0.3;
        animation: float 6s ease-in-out infinite;
    }

    .floating-leaf:nth-child(1) {
        top: 10%;
        left: 10%;
        animation-delay: 0s;
    }

    .floating-leaf:nth-child(2) {
        top: 20%;
        right: 15%;
        animation-delay: 1s;
    }

    .floating-leaf:nth-child(3) {
        bottom: 30%;
        left: 20%;
        animation-delay: 2s;
    }

    .floating-leaf:nth-child(4) {
        bottom: 20%;
        right: 10%;
        animation-delay: 3s;
    }

    .floating-leaf:nth-child(5) {
        top: 50%;
        left: 50%;
        animation-delay: 4s;
    }

    @@keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        33% { transform: translateY(-20px) rotate(5deg); }
        66% { transform: translateY(-10px) rotate(-3deg); }
    }

    /* Empty State Styling */
    .empty-state {
        text-align: center;
        padding: 80px 20px;
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .empty-state i {
        margin-bottom: 20px;
    }

    .empty-state h3 {
        font-size: 1.8rem;
        margin-bottom: 15px;
        font-family: 'Playfair Display', serif;
    }

    .empty-state p {
        font-size: 1.1rem;
        margin-bottom: 30px;
    }

    .empty-state .btn {
        padding: 12px 30px;
        font-weight: 600;
        border-radius: 25px;
        background: linear-gradient(135deg, #8BC34A 0%, #689F38 100%);
        border: none;
        transition: all 0.3s ease;
    }

    .empty-state .btn:hover {
        background: linear-gradient(135deg, #689F38 0%, #558B2F 100%);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(139, 195, 74, 0.4);
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .testimonial-main-title {
            font-size: 2rem;
        }

        .trust-indicator {
            padding: 25px 15px;
            margin-bottom: 20px;
        }

        .floating-leaf {
            font-size: 1.5rem;
        }

        /* Individual testimonial cards mobile */
        .individual-testimonial-card {
            padding: 20px;
            margin-bottom: 20px;
        }

        .customer-avatar {
            width: 50px;
            height: 50px;
            margin-right: 12px;
        }

        .customer-name {
            font-size: 1.1rem;
        }

        .testimonial-description {
            font-size: 0.95rem;
        }

        .featured-review-badge {
            font-size: 0.7rem;
            padding: 6px 12px;
        }
    }
</style>

<!-- Include Owl Carousel for testimonials -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.min.css">

<script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>
<script>
    $(document).ready(function(){
        $('.testimonials-carousel').owlCarousel({
            loop: true,
            margin: 20,
            nav: false,
            dots: true,
            autoplay: true,
            autoplayTimeout: 5000,
            autoplayHoverPause: true,
            responsive: {
                0: {
                    items: 1
                },
                768: {
                    items: 2
                },
                1000: {
                    items: 3
                }
            }
        });
    });
</script>
</section>

<script>
// Add smooth scrolling animation for testimonial cards
document.addEventListener('DOMContentLoaded', function() {
    const cards = document.querySelectorAll('.testimonial-card');
    
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };
    
    const observer = new IntersectionObserver(function(entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);
    
    cards.forEach(card => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(30px)';
        card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
        observer.observe(card);
    });
});
</script>
